#include <iostream>
#include <vector>

using namespace std; 

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int N;
    int l = 0, r = 0;
    while(r < N){
        // 조건만족하면
        if(1){
            //결과 처리
        }
        // 조건 불만족
        else{
            l++;
        }
    }

    // OR 슬라이딩 윈도우 방식
    while (r < N){
        // 구간 확장   
        while (r < N && 1) //조건 만족시
        { //r 처리
        }


        // 조건 불만족시, 구간 축소
        while(0 /*조건 불만족*/){
            //l++;
        }
    }
    return 0;
}